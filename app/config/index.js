export default {
    // DEV
  schema: 'http',
  apiHost: 'dam.dev.vcg.com:8088/api',
  sameOrigin: false, //When we depoy the web client
  //just under the same domain with api server, Set to TRUE.
  //Other config will not effect anymore.
};
